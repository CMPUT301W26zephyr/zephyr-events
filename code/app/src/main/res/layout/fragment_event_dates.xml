<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <include
        android:id="@+id/top_bar"
        layout="@layout/layout_top_bar"
        app:layout_constraintTop_toTopOf="parent" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/top_bar"
        app:layout_constraintBottom_toTopOf="@+id/bottom_button_container">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <LinearLayout style="@style/FormLabelRow" android:layout_marginTop="0dp">
                <TextView style="@style/FormAsterisk" />
                <TextView style="@style/FormLabel" android:text="Registration Open Date" />
            </LinearLayout>
            <EditText
                android:id="@+id/input_reg_start_date"
                style="@style/FormInput"
                android:hint="Select date"
                android:focusable="false"
                android:drawableEnd="@drawable/ic_calendar" />

            <LinearLayout style="@style/FormLabelRow">
                <TextView style="@style/FormAsterisk" />
                <TextView style="@style/FormLabel" android:text="Event Start Date" />
            </LinearLayout>
            <EditText
                android:id="@+id/input_event_start_date"
                style="@style/FormInput"
                android:hint="Select date"
                android:focusable="false"
                android:drawableEnd="@drawable/ic_calendar" />

            <LinearLayout style="@style/FormLabelRow">
                <TextView style="@style/FormAsterisk" />
                <TextView style="@style/FormLabel" android:text="Event End Date" />
            </LinearLayout>
            <EditText
                android:id="@+id/input_event_end_date"
                style="@style/FormInput"
                android:layout_marginBottom="24dp"
                android:hint="Select date"
                android:focusable="false"
                android:drawableEnd="@drawable/ic_calendar" />

        </LinearLayout>
    </ScrollView>

    <FrameLayout
        android:id="@+id/bottom_button_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent">
        <include
            android:id="@+id/next_button"
            layout="@layout/button_primary" />
    </FrameLayout>
</androidx.constraintlayout.widget.ConstraintLayout>