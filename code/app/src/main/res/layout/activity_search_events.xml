<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:paddingStart="18dp"
    android:paddingEnd="18dp">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="48dp"
        android:layout_height="?attr/actionBarSize"
        android:background="@android:color/white"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:navigationIcon="@drawable/outline_arrow_back_ios_24" />

    <EditText
        android:id="@+id/etSearchBar"
        android:layout_width="0dp"
        android:layout_height="44dp"
        android:layout_marginStart="12dp"
        android:background="@drawable/bg_card_white"
        android:drawableStart="@drawable/outline_feature_search_24"
        android:drawablePadding="8dp"
        android:gravity="center_vertical"
        android:hint="Search events"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        app:layout_constraintStart_toEndOf="@id/toolbar"
        app:layout_constraintEnd_toStartOf="@id/btnSearchFilter"
        app:layout_constraintTop_toTopOf="@id/toolbar"/>

    <!-- Filter icon -->
    <FrameLayout
        android:id="@+id/btnSearchFilter"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/toolbar">
        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center"
            android:padding="6dp"
            android:src="@drawable/outline_filter_alt_24"
            app:tint="@color/black"/>
    </FrameLayout>

    <!-- Your history title -->
    <TextView
        android:id="@+id/txtSearchHistoryTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Your History"
        android:textColor="@color/black"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/etSearchBar"/>

    <!-- History tags row with clear X -->
    <HorizontalScrollView
        android:id="@+id/scrollHistory"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:scrollbars="none"
        android:clipToPadding="false"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btnClearSearch"
        app:layout_constraintTop_toBottomOf="@id/txtSearchHistoryTitle">

        <LinearLayout
            android:id="@+id/historyTagsContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingEnd="8dp"/>
    </HorizontalScrollView>

    <!-- Clear history X button -->
    <ImageView
        android:id="@+id/btnClearSearch"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:padding="4dp"
        android:src="@drawable/outline_close_24"
        app:tint="@color/black"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/scrollHistory"
        app:layout_constraintBottom_toBottomOf="@id/scrollHistory"/>

    <!-- Content area (for search results; populate from backend) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvContent"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:paddingTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/scrollHistory"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>











</androidx.constraintlayout.widget.ConstraintLayout>