package com.example.zephyrevents.model;

/**
 * Enum to represent the State of a User's Entry in a Waitlist
 */
public enum Status {
    WAITLISTED,
    SELECTED,
    ACCEPTED,
    DECLINED,
    REMOVED,
    LOST
}
