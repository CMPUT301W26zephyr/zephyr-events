# Generating Javadoc

Make the script executable (only needed once):

chmod +x ./scripts/gen-javadoc.sh

Generate and open docs:

./scripts/gen-javadoc.sh

Open docs without regenerating:

./scripts/gen-javadoc.sh -o

Docs are generated in:
javadoc/index.html
